# Frontend — Alerta Ciudadana

Pendiente. Aquí montaremos la app con **React + Vite + TypeScript** en un día
posterior. Por ahora la carpeta queda reservada para mantener la estructura del
proyecto (`backend/` + `frontend/`).
